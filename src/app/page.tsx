import Image from "next/image";

export default function Home() {
  return (
    <main>
      <section
        id="hero"
        className="min-h-screen flex items-center justify-center"
      >
        <h1 className="text-[clamp(3rem,10vw,10rem)] font-bold leading-[0.85] tracking-[-0.04em]">
          FARID ZAHRAN
        </h1>
      </section>
    </main>
  );
}
